# Enterprise plugins live here.
#
# Each package is a standalone pnpm workspace member published under
# the `@objectos/plugin-*` scope. They are composed into the runtime
# manifest from `apps/objectos/objectstack.config.ts`.
#
# Naming: `plugin-sso-saml`, `plugin-scim`, `plugin-audit-export`,
#         `plugin-ha-scheduler`, ...
